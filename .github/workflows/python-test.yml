name: Python Tests

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: [3.7, 3.8, 3.9, 3.10]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest
        pip install setuptools

    - name: Run tests
      run: |
        pytest test/GatherInfoTest.py
        pytest test/FileOperationsTest.py
        pytest test/LoggingTest.py
        pytest test/SynchronizeTest.py
        pytest test/CallBashTest.py
        pytest test/BackupTest.py

      # Display test results
      continue-on-error: false
